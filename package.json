{
  "name": "webtools",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Free, open-source, client-side developer tools — JSON, URL, encoding, hashing, and more.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"test/**/*.test.js\"",
    "prices": "node scripts/update-prices.mjs"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  }
}
